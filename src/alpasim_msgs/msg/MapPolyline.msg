# Ordered 3D points expressed in VectorMap.frame_id
geometry_msgs/Point[] points

# Optional heading in radians.
# Empty if unavailable.
# When populated, its length equals points length.
float64[] headings
